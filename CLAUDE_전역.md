<!-- 생성: 2026-09-20 18:58 (KST) -->
<!-- C:\Users\kyssk\.claude\CLAUDE.md 의 사본. 원본이 바뀌면 다시 복사해야 한다. -->

# 전역 지침

## 응답
- 한국어로 답한다. 코드, 명령어, 파일 경로는 원문 그대로 둔다.
- 결론을 먼저 말하고 근거를 뒤에 붙인다. 추측과 확인된 사실을 구분해서 말한다.

## 작업 방식
- 코드를 고치기 전에 무엇을 어떻게 바꿀지 먼저 설명하고 승인을 받는다.
- 새 기능이나 버그 수정은 테스트를 먼저 작성한다(TDD).
- 오류가 나면 원인을 먼저 찾고, 추측으로 고치지 않는다.
- 요청한 범위만 고친다. 관련 없는 파일은 건드리지 않는다.

## 확인
- "됐다"고 말하기 전에 실제로 실행해서 결과를 확인한다.
- 테스트가 실패하면 실패했다고 그대로 보고한다.

## git
- main 브랜치에 바로 커밋하지 않고 작업 브랜치를 만든다.
- 커밋과 push는 사용자가 요청할 때만 한다.

## 파일 작성
- 새로 만드는 파일은 맨 위에 생성 날짜와 시각을 주석으로 적는다. 예: `# 생성: 2026-09-20 18:55 (KST)`
- 시각은 대한민국 표준시(KST, UTC+9) 기준이며, 추측하지 말고 시스템 시각을 확인해서 적는다.
- 주석 문법은 파일 형식에 맞춘다. .py/.ps1/.sh는 `#`, .md는 HTML 주석, .json처럼 주석을 지원하지 않는 형식은 생략한다.
- 기존 파일을 수정할 때는 날짜 주석을 새로 넣지 않는다. 새로 만드는 파일에만 적용한다.
